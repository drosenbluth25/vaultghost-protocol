# Fixtures — Pointer Index

The fixture ZIPs named in the integration prompt are not vendored in this branch because the ZIP files were not supplied to this workspace. The integration prompt requires that fixture ZIP contents not be altered, so no surrogate ZIP is created here.

| Expected artifact | Expected path | SHA-256 status |
|---|---|---|
| Reproducible Unsigned Fixture v0.1.5 | `vaultghost_v015_reproducible_fixture.zip` | Pending — fixture ZIP not supplied to this workspace |
| Real Signed Fixture v0.4.5 | `vaultghost_v045_real_signed_fixture.zip` | Pending — fixture ZIP not supplied to this workspace |

When the fixture ZIPs are supplied, drop them into this directory unaltered and update `../artifacts-manifest.md` with computed SHA-256 values.

Internal fixture values referenced in the supplied counsel-intake packet are recorded in `../counsel-intake-baseline.md` and `../artifacts-manifest.md` for counsel intake.
